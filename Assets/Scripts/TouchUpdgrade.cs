using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TouchUpdgrade : MonoBehaviour
{
    // Start is called before the first frame update}
    public int touchs = 1;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
